## artifact-summary cycle-4 (post-fix-3) — final

ЦИКЛ 4: применены правки cycle-3 (Корректность/Реализуемость/Риски FAIL 2:1). Ключевые изменения cycle-3→cycle-4:

### Изменения cycle-3 → cycle-4
- **[fix-3.1] Stryker cross-platform + comma format**: `git diff | xargs -r npx stryker run --mutate` → `node scripts/stryker-incremental.mjs` (cross-platform; `xargs -r` GNU-only, macOS BSD не поддерживает; Stryker 8.x `--mutate` ожидает comma-separated `file1.ts,file2.ts`, не space-separated). Во всех: tests.md шаг 8, test-matrix.md T2 Eval, Makefile gate-mutation, standard.md §10. Fallback `git merge-base HEAD main` если `origin` не настроен.
- **[fix-3.2] cockatiel CB errorFilter**: `CircuitBreaker` теперь имеет `shouldErrorBeReportedAsFailure: (err) => isTransient(err) && !AbortError` — CB не считает failure для cancellation/4xx/DomainError (иначе ложное OPEN). В resilience.md stack binding + good-пример.
- **[fix-3.3] Bulkhead→Timeout порядок + CB↔Bulkhead sizing-инвариант**: per-attempt Timeout innermost к операции (время в очереди Bulkhead НЕ расходует per-attempt timeout-бюджет); CB threshold ↔ Bulkhead maxConcurrent калибруются совместно (CB срабатывает РАНЬШЕ исчерпания Bulkhead при 10× RPS). В resilience.md.
- **[fix-3.4] contracts.md ≥80 строк**: добавлены C5 (контракт-data schema↔Drizzle) + gate 6 (schema-contract-gate: grep прямого возврата db.select() row наружу). 81 строка.
- **[fix-3.5] event-loop lag threshold из thresholds**: `thresholds.yaml performance.event_loop_lag_p99_ms` (не хардкод 50). В performance.md P4.
- **[fix-3.6] k6 10× RPS scenario явный**: `scripts/k6-10x.js` — baseline RPS × 10 из thresholds.yaml, thresholds p99<hot_p99_ms.prod, duration 1m. В performance.md P4.
- **[fix-3.7] Makefile preflight**: внешние инструменты (docker, k6, toxiproxy-cli, pg_dump) проверяются, WARN если отсутствуют. toxiproxy через Docker ghcr.io/shopify/toxiproxy; Pact нативные бинари через npm install.

### Полный инвариантный набор (cycle-4 final)
- 6 skills: 10 секций контракта каждый (назначение/когда · входы/выходы · non-negotiables · workflow domain→db→service→api→tests→docs; persistence N/A · stack binding без плейсхолдеров стека · quality gates реальные команды · bad/good ❌/✅ · DoD · stop/escalation S1–S6 · audit trail). Длины 123–223.
- 5 rules: одна тема каждый 81–128 строк (80–250 диапазон; contracts 81 ≥80).
- Stack (cycle-4): Node 24, npm, TS strict, Vitest+coverage-v8, fast-check, Stryker (incremental via node scripts/stryker-incremental.mjs, comma-separated --mutate, cross-platform), Drizzle 0.36+ + postgres-js + drizzle-kit (forward-only + вручную down.sql), @testcontainers/postgresql, cockatiel (wrap order: outer AbortController total deadline → CB(shouldErrorBeReportedAsFailure=isTransient&&!AbortError) → Retry(fullJitter(exponentialBackoff)) → Bulkhead + per-attempt Timeout innermost; CB↔Bulkhead sizing-инвариант), undici (Agent headersTimeout/bodyTimeout/connections), pino+pino-http, zod 3.x (.strict()↔additionalProperties:false, @asteasolutions/zod-to-openapi@^7), @redocly/cli, oasdiff, husky v9+lint-staged+commitlint (custom plugin commitlint-plugin-contribution-tag), eslint+typescript-eslint+eslint-plugin-security/promise/import, prettier, gitleaks, Semgrep, k6 (10× RPS scenario scripts/k6-10x.js), toxiproxy (network-level, Docker) vs undici MockAgent (HTTP-level), perf_hooks.monitorEventLoopDelay (event-loop gate, threshold из thresholds.yaml, не clinic doctor), prom-client, piscina, Pact (нативные бинари через npm).
- Sizing (P5): pg pool / undici Agent / Bulkhead / p-limit / piscina / k6 / event-loop lag — все из thresholds.yaml performance, не хардкод (gate-sizing node scripts/sizing-check.mjs).
- Паритет: resilience.md ↔ feature.md шаг 4 ↔ database.md шаг 9 ↔ review.md блок 2 — cockatiel wrap order (CB→Retry с errorFilter), fullJitter(exponentialBackoff), total deadline AbortController, idempotency onConflictDoNothing. test-matrix.md ↔ tests.md ↔ feature.md ↔ standard.md §10 ↔ Makefile — Stryker incremental (node script, comma --mutate), toxiproxy/MockAgent разделение. vcs.md ↔ contracts.md C1/C5 ↔ feature.md workflow — спека раньше кода, commitlint custom plugin.
- Плейсхолдеров `<ВАШ_СТЕК>`/`<ТЕСТ_РАННЕР>`/`<ORM>`/стек-`<...>` НЕТ. `<TICKET>`/`<slug>`/`<type>`/`<scope>`/`<subject>` — переменные значений (как в исходном harness vcs.md), НЕ стек-плейсхолдеры. Учебных леса «Глоссарий»/«Форма правила»/«Что ломается»/`GATE:` НЕТ. Длины: skills 123–223, agents 81–128, Makefile 191.
